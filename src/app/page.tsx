import StartMenu from "@/components/menu/start-menu/StartMenu";

export default function StartPage() {
    return (
        <div>
            <StartMenu/>
            <h1>
                Welcome to our website.
                To view content and further work with site, you need to authenticate.
            </h1>
        </div>
    );
}
